# 星地量子密钥分发链路仿真与安全分析系统
## 项目完整方案规划书

---

## 一、项目概述

### 1.1 项目背景
量子密钥分发（QKD）利用量子力学原理实现无条件安全的密钥分发。卫星-地面自由空间QKD是构建全球量子通信网络的关键技术。然而，大气信道（天气、湍流）和潜在窃听者（Eve）对链路安全性和可用性产生重大影响。

### 1.2 项目目标
构建一个集成真实卫星轨道、实时气象数据、完整QKD协议模拟和攻击-防御体系的可交互仿真平台，为星地量子通信链路规划提供工程决策支持。

### 1.3 核心创新点
1. **真实数据驱动**：集成真实卫星轨道（TLE）和实时气象数据（Open-Meteo）
2. **完整攻防体系**：实现3类典型攻击（截获-重发、光束分离、PNS）及对应防御机制
3. **自适应安全决策**：基于信道条件动态调整安全策略，平衡通信效率与安全性
4. **可视化链路状态**：实时展示卫星轨迹、信道质量、安全状态

---

## 二、系统架构设计

### 2.1 整体架构

```
用户交互层 (Gradio)
├── 地图可视化：卫星轨迹、星下点、地面站、通信链路
├── 参数控制：时间选择、攻击类型、防御开关、天气场景
└── 结果展示：QKD指标、安全状态、密钥率曲线

核心仿真引擎
├── 轨道动力学模块（用户已有）
├── 气象数据模块（用户已有）
├── QKD协议模块（BB84实现）
├── 攻击模拟模块（3类攻击）
└── 安全决策模块（防御+决策）

数据与可视化层
├── 实时计算：距离、透射率、误码率、密钥率
├── Plotly图表：时序曲线、对比分析、安全区域图
└── 日志记录：仿真结果导出、报告生成
```

### 2.2 模块划分与接口定义

#### 模块1：轨道动力学模块（已有，需适配）
**文件**: orbit_module.py

**输入接口**:
- get_satellite_position(tle_data, timestamp) -> dict with lat, lon, alt, subpoint
- calculate_slant_range(sat_pos, ground_station) -> dict with distance_km, elevation_deg, azimuth_deg

**已有功能确认**:
- [x] 基于TLE的轨道传播
- [x] 星下点计算
- [x] Web可视化（可拖动时间轴）

**需新增**:
- [ ] 斜距和仰角计算（用于大气路径长度）
- [ ] 批量时间序列生成（用于过境动画）

---

#### 模块2：气象数据模块（已有，需适配）
**文件**: weather_module.py

**输入接口**:
- get_weather_data(lat, lon, timestamp) -> dict with temperature, humidity, visibility_km, weather_code
- weather_to_attenuation(weather_data) -> dict with attenuation_db_per_km, turbulence_cn2, weather_type

**已有功能确认**:
- [x] Open-Meteo API调用
- [x] 实时天气获取

**需新增**:
- [ ] 天气代码到衰减系数的映射表
- [ ] 斜路径衰减计算（考虑仰角）

**衰减系数映射表**:
- clear (visibility > 20km): 0.2 dB/km
- mist (5-20km): 2.0 dB/km
- fog (1-5km): 10.0 dB/km
- rain: 5.0 dB/km
- snow: 8.0 dB/km

---

#### 模块3：QKD协议核心模块（新建）
**文件**: qkd_core.py

**类定义**:
- BB84SatelliteQKD: 主仿真器类
  - __init__(distance_km, elevation_deg, attenuation_db_per_km, alice_pulse_rate=1e6)
  - calculate_channel_transmission() -> float
  - simulate_exchange(n_pulses=10000, eve_attack=None) -> dict

- SiftingProtocol: 筛选协议
- ErrorCorrection: 误码纠错（Cascade协议简化版）

**关键算法**:
1. 信道透射率 = 自由空间损耗 × 大气衰减 × 光学效率
2. BB84模拟：光子发射（泊松分布）→ 信道传输 → Bob测量 → 筛选 → QBER计算

---

#### 模块4：Eve攻击模块（新建）
**文件**: eve_attacks.py

**攻击类型**:
1. **InterceptResendAttack**: 截获-重发，引入25%误码
2. **BeamSplittingAttack**: 光束分离，不引入误码但泄露信息
3. **PNAttack**: 光子数分离攻击，针对弱脉冲光源

**每个攻击类实现**:
- attack(photon_stream, alice_bases, alice_bits) -> dict
  - photon_to_bob: 给Bob的光子
  - eve_info_ratio: Eve知道的信息比例
  - introduced_qber: 引入的额外误码

---

#### 模块5：安全防御与决策模块（新建）
**文件**: security_defense.py

**类定义**:
- PrivacyAmplification: 隐私放大
  - calculate_secure_length(n_sifted, eve_info_ratio) -> int
  - amplify(sifted_key, target_length) -> secure_key

- DecoyStateProtocol: 诱骗态协议
  - generate_pulse_sequence(n_pulses) -> (intensities, types)
  - detect_pns_attack(signal_yield, decoy_yield, vacuum_yield) -> dict

- SecurityDecisionEngine: 安全决策引擎
  - make_decision(qber, photon_rate, eve_info, n_sifted) -> dict
  - 阈值: max_qber=0.11, min_photon_rate=100, max_eve_info=0.3

---

#### 模块6：可视化与界面模块（新建）
**文件**: app.py

**Gradio界面**:
- 左侧控制面板：卫星选择、地面站、时间滑块、攻击类型、防御开关
- 右侧可视化：
  - 地图轨迹图（Plotly Scattergeo）
  - 性能指标时序图（双Y轴：密钥率+QBER）
  - 安全状态分析图（Eve信息vs安全密钥）
- 底部：详细数据表格

---

## 三、开发计划与里程碑

### 3.1 开发阶段

#### Phase 1: 核心引擎（Week 1, 6小时）
**目标**：QKD协议和攻击防御模块可独立运行

| 任务 | 文件 | 时间 | 验收标准 |
|------|------|------|---------|
| BB84协议实现 | qkd_core.py | 2h | 命令行可运行，输出QBER和密钥率 |
| 3类Eve攻击 | eve_attacks.py | 2h | 每种攻击可独立实例化和运行 |
| 防御机制 | security_defense.py | 2h | 隐私放大和诱骗态可正确计算 |

**交付物**:
```
python test_qkd.py
输出示例：
- 无攻击: QBER=3.2%, 密钥率=45 kbps
- 截获重发(30%): QBER=10.8%, 密钥率=12 kbps, 检测到攻击
- 光束分离(20%): QBER=3.2%, 密钥率=28 kbps, 隐私放大有效
```

#### Phase 2: 数据集成（Week 2, 6小时）
**目标**：接入真实轨道和天气数据

| 任务 | 文件 | 时间 | 验收标准 |
|------|------|------|---------|
| 适配轨道模块 | orbit_adapter.py | 2h | 输入时间→输出距离和仰角 |
| 适配天气模块 | weather_adapter.py | 2h | 输入坐标→输出衰减系数 |
| 信道模型整合 | channel_model.py | 2h | 综合距离、仰角、天气计算透射率 |

#### Phase 3: 界面开发（Week 3, 8小时）
**目标**：可交互的Web应用

| 任务 | 文件 | 时间 | 验收标准 |
|------|------|------|---------|
| Gradio基础界面 | app.py | 3h | 所有控件可显示，事件可触发 |
| Plotly图表集成 | visualization.py | 3h | 三张图可正确显示数据 |
| 用户交互优化 | app.py | 2h | 时间轴拖动实时更新，动画播放 |

#### Phase 4: 测试与文档（Week 4, 6小时）
**目标**：完整测试和报告撰写

| 任务 | 时间 | 验收标准 |
|------|------|---------|
| 集成测试 | 2h | 全流程无报错，边界条件处理正确 |
| GitHub整理 | 1h | README完整，commit历史清晰 |
| 报告撰写 | 3h | 按模板完成所有章节 |

### 3.2 详细时间表

```
Week 1 (6小时):
- Day 1 (2h): BB84协议核心
- Day 2 (2h): Eve攻击体系
- Day 3 (2h): 防御机制

Week 2 (6小时):
- Day 1 (2h): 轨道数据适配
- Day 2 (2h): 天气数据适配
- Day 3 (2h): 信道模型整合

Week 3 (8小时):
- Day 1-2 (6h): Gradio界面
- Day 3 (2h): Plotly可视化

Week 4 (6小时):
- Day 1 (2h): 测试
- Day 2 (1h): GitHub
- Day 3 (3h): 报告
```

---

## 四、技术规格与约束

### 4.1 性能指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 仿真计算延迟 | <500ms | 单次QKD模拟（10000脉冲） |
| 界面响应延迟 | <100ms | 时间轴拖动到图表更新 |
| 支持时间点数 | >1000 | 单次过境动画流畅度 |

### 4.2 依赖库

```
numpy>=1.21.0
scipy>=1.7.0
gradio>=3.50.0
plotly>=5.0.0
pandas>=1.3.0
requests>=2.26.0
skyfield>=1.42
```

### 4.3 文件结构

```
qkd-satellite-sim/
├── README.md
├── requirements.txt
├── app.py
├── config.py
├── modules/
│   ├── __init__.py
│   ├── qkd_core.py
│   ├── eve_attacks.py
│   ├── security_defense.py
│   ├── channel_model.py
│   ├── orbit_adapter.py
│   ├── weather_adapter.py
│   └── visualization.py
├── tests/
│   ├── test_qkd.py
│   ├── test_attacks.py
│   └── test_defense.py
├── data/
│   └── sample_tle.txt
└── docs/
    └── report.pdf
```

---

## 五、验收标准

### 5.1 功能验收

| 功能 | 测试方法 | 通过标准 |
|------|---------|---------|
| 基础QKD | 命令行运行test_qkd.py | QBER<5%时密钥率>0 |
| 攻击检测 | 启用截获重发攻击 | QBER>11%时标记攻击 |
| 防御有效 | 光束分离+隐私放大 | 最终密钥率>0且Eve信息≈0 |
| 地图显示 | 拖动时间轴 | 卫星位置实时更新 |
| 天气集成 | 选择不同城市 | 天气数据正确影响结果 |

### 5.2 报告要求

按模板完成以下章节：
1. **作品功能与性能说明**：系统截图，功能列表，性能指标表
2. **设计与实现方案**：
   - 2.1 实现原理：系统架构图、QKD流程图、攻击-防御流程图
   - 2.2 运行结果：界面截图、典型场景数据
   - 2.3 技术指标：密钥率、QBER、安全距离等数据表
3. **系统测试与结果**：测试用例设计、测试结果数据
4. **应用前景**：星地量子通信网络规划、地面站选址
5. **结论**：创新点总结、局限性、未来工作

---

## 六、风险与应对

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|---------|
| Open-Meteo API不稳定 | 中 | 天气功能失效 | 缓存典型天气数据作为fallback |
| 轨道计算精度不足 | 低 | 距离误差大 | 使用skyfield库，验证TLE数据 |
| QKD物理模型过于简化 | 中 | 被质疑专业性 | 报告明确说明简化假设，引用文献 |
| 时间不足 | 中 | 功能裁剪 | 优先保证基础QKD+界面，攻击类型可裁剪 |

---

## 七、关键公式汇总

**信道透射率**:
```
T = T_fs × T_atm × η_opt
T_fs = (λ/4πd)²
T_atm = 10^(-α×d/(10×sin(θ)))
```

**安全密钥率（简化GLLP）**:
```
r = 1 - 2×H2(QBER)
R = r × R_sifted
H2(x) = -x log2(x) - (1-x) log2(1-x)
```

**隐私放大**:
```
m = n × (1 - 2τ) - 2 log2(1/ε)
```

---

**文档版本**: v1.0
**编制日期**: 2026-03-26
