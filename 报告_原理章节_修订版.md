# 作品设计报告 —— 原理章节（修订版）

> 本文档可直接作为报告第2章"设计与实现方案"的内容素材。全文不含代码，仅包含原理、公式与系统架构说明。

---

## 2. 设计与实现方案

### 2.1 实现原理

#### （1）系统架构

系统采用分层模块化架构，自上而下分为三层：用户交互层、核心仿真引擎层、数据与可视化层。

- **用户交互层**：基于 Gradio 框架构建 Web 界面，提供参数控制与结果可视化。
- **核心仿真引擎层**：包含轨道动力学模块、气象数据模块、信道传输模型、BB84 协议模块、Eve 攻击模拟模块、安全决策模块六大核心组件。
- **数据与可视化层**：负责实时计算链路参数并生成 Plotly 图表。

模块间数据流为：TLE 星历与气象 API → 轨道/气象模块 → 信道模型计算透射率 → BB84 协议模块（可选插入 Eve 攻击）→ 安全决策模块（隐私放大/诱骗态检测）→ 可视化输出。

#### （2）BB84 协议仿真流程

BB84 协议由 Bennett 和 Brassard 于 1984 年提出，利用量子力学基本原理实现信息论安全的密钥分发。系统仿真严格遵循协议四步：

**Step 1：Alice 制备**
Alice 随机生成比特序列 $a_i \in \{0,1\}$ 和基序列 $b_i^{(A)} \in \{Z,X\}$，其中 Z 基编码 $\{|0\rangle,|1\rangle\}$，X 基编码 $\{|+\rangle,|-\rangle\}$。每个光脉冲的光子数服从泊松分布 $n_i \sim \text{Poisson}(\mu)$，$\mu$ 为平均光子数（系统取 0.8）。

**Step 2：信道传输**
光子通过星地自由空间信道传输。信道透射率 $T$ 由三部分构成：

$$T = T_{\text{fs}} \times T_{\text{atm}} \times \eta_{\text{opt}}$$

其中 $T_{\text{fs}}$ 为自由空间损耗（按距离平方反比缩放），$T_{\text{atm}}$ 为大气衰减（基于仰角修正的有效路径计算），$\eta_{\text{opt}}$ 为系统光学效率。

**Step 3：Bob 测量**
Bob 随机选择基进行测量。探测器以效率 $\eta_{\text{det}}$（40%）响应光子，暗计数率 $R_{\text{dark}}$（50 Hz）引入额外噪声。

**Step 4：筛选与后处理**
- **筛选（Sifting）**：保留基匹配且探测器响应的比特；
- **误码计算**：统计筛选后比特不一致比例，得到量子误码率 QBER；
- **隐私放大**：根据 QBER 和 Eve 信息比例压缩密钥，得到最终安全密钥。

**泊松光源模型**
实际 QKD 系统多采用弱相干光脉冲模拟单光子源，光子数分布为：

$$P(n) = \frac{\mu^n e^{-\mu}}{n!}$$

$\mu$ 的选取需权衡计数率与多光子比例：$\mu$ 过大则多光子态比例升高（易受 PNS 攻击），过小则探测计数率低。

**QBER 来源**
量子误码率由三部分组成：

$$\text{QBER}_{\text{total}} = \text{QBER}_{\text{optical}} + \text{QBER}_{\text{dark}} + \text{QBER}_{\text{Eve}}$$

光学误码（偏振漂移、对准误差）约 0.5%；暗计数误码在低透射率信道下成为主导；攻击引入误码因攻击类型而异。

#### （3）攻击-防御体系

系统实现了三类典型量子密码攻击及配套防御机制，构成完整的攻防闭环。

**截获-重发攻击（Intercept-Resend）**
Eve 以概率 $p_{\text{attack}}$ 拦截光脉冲，随机选择基测量后重新制备光子发送给 Bob。当 Eve 的基与 Alice 不同时，引入 50% 误码；基一致时不引入误码。平均引入误码为 $0.25 \cdot p_{\text{attack}}$。此类攻击最显著的特征是 **QBER 异常升高**，当 QBER 超过 11% 时，BB84 协议无法提取安全密钥，通信双方可据此检测攻击。

**光束分离攻击（Beam-Splitting）**
Eve 在信道中插入分束器，分离出比例 $r$ 的光强进行测量，其余转发给 Bob。关键特性是**不引入误码**，Bob 收到的光子态未被破坏，因此无法通过 QBER 检测。防御必须依赖**隐私放大**：通过压缩密钥长度消除 Eve 的信息优势。

**光子数分离攻击（PNS）**
针对弱脉冲光源的多光子态缺陷。Eve 测量每个脉冲的光子数，对 $n \geq 2$ 的多光子态分离出一个光子存入量子存储器，待 Alice 和 Bob 公开基选择后，用正确基测量存储光子，从而**无误差地获取信息**。

**PNS 攻击的核心特殊性**：它不引入误码，QBER 不会升高。这意味着**如果没有诱骗态协议，系统完全无法察觉 PNS 攻击的存在**，会继续生成密钥，但所有密钥在信息论上均不安全。本系统对此做了严格处理：当遭受 PNS 攻击但未启用诱骗态时，安全密钥率强制归零，并在报告中输出明确警告。

**诱骗态协议（Decoy State Protocol）**
诱骗态是检测 PNS 攻击的核心手段。Alice 随机发射三种强度脉冲：信号态（强度 $\mu$，用于生成密钥）、诱骗态（强度 $\nu < \mu$）、真空态（强度 0）。PNS 攻击会改变不同强度脉冲的产额（探测概率）比例：Eve 倾向于对多光子态分离光子，导致信号态与诱骗态的产额比异常升高。系统通过监测产额比检测攻击，当产额比超过预期值 1.2 倍时报警。

检测到 PNS 攻击后，系统不直接中断通信，而是改用 **GLLP 保守公式** 计算安全密钥率——假设所有多光子态均被 Eve 拿到，只从单光子贡献部分提取安全密钥。

**隐私放大（Privacy Amplification）**
即使 Eve 通过光束分离等无噪攻击获取了部分信息，通信双方仍可通过压缩密钥长度，使 Eve 对最终密钥的信息量趋近于零。本系统采用 GLLP 简化公式计算安全密钥长度：

$$m = n \times [1 - 2H_2(\text{QBER}) - 2\tau]$$

其中：
- $n$：筛选后密钥长度；
- $H_2(x) = -x\log_2 x - (1-x)\log_2(1-x)$：二元熵函数；
- $2H_2(\text{QBER})$：第一项为纠错过程需公开的奇偶校验信息，第二项为消除"Eve 听到纠错过程"带来的信息泄露；
- $2\tau$：Eve 信息比例的保守消除开销（$\tau$ 为 Eve 已知信息比例）。

隐私放大的信息论本质是利用**通用哈希函数**（或 Toeplitz 矩阵）将长串映射为短串。每个输出位由大量输入位的随机线性组合（XOR）构成。Eve 即使知道部分原始位，由于不知道组合方式，也无法推断出任何一位安全密钥——这类似于"将部分信息通过压缩变为无信息"。

**安全阈值的二元判定**
BB84 协议的安全判定是严格的二元阈值：
- **QBER < 11%**：该时刻可通过隐私放大提取安全密钥；
- **QBER $\geq$ 11%**：该时刻**完全不可行**（Holevo 界限制），安全密钥率强制归零。

系统因此采用基于**硬阈值**的安全等级评估，而非连续打分。对于一次卫星过境（含数千个时间点），系统统计 QBER < 11% 的时间占比作为"安全时段"，并同时显示 QBER 的最小值、最大值与范围，避免平均值掩盖极端时刻的安全风险。

#### （4）隐私放大教学演示模块

为直观展示隐私放大的信息论本质，系统新增了独立的教学演示界面。该模块以可视化方式呈现：

- **三视角密钥对比**：分别展示 Alice 的原始密钥、Eve 的已知信息（未知位标记为"?"）、Bob 的含误码密钥；
- **颜色标注体系**：绿色表示仅 Alice/Bob 知道，红色表示 Eve 也知道，黄色表示 Alice/Bob 不一致（误码）；
- **随机线性组合过程**：逐行展示每个安全密钥位由哪些原始位 XOR 组合得到；
- **信息论公式推导**：实时计算并展示 $m = n \times [1 - 2H_2(\text{QBER}) - 2\tau]$ 的每一步；
- **信息分布条形图**：用 Plotly 图表对比原始密钥、Eve 知道的位数、误码位数、压缩损失与最终安全密钥长度。

#### （5）与课程知识的内在关联

本作品虽聚焦于量子密码前沿方向，但其核心思想、分析方法与工具均源于《密码学导论》课程内容：

- **与完善保密性（Perfect Secrecy）的关联**：课程中一次性便笺（OTP）的完善保密性基于密钥真随机性；BB84 利用量子测量塌缩产生真随机密钥，实现信息论意义下的无条件安全。二者都是"不依赖计算困难性"的安全。
- **与密码分析技术的关联**：课程要求"养成从攻击角度质疑的习惯"。本作品从 Eve 视角实现了三类典型攻击并量化其影响：截获-重发对应主动篡改/中间人攻击思想，光束分离对应被动监听与信息泄露分析，PNS 对应实现漏洞分析。
- **与信息论基础的关联**：隐私放大直接使用课程中的二元熵函数 $H_2(x)$ 和熵的概念；诱骗态检测的产额比分析本质是概率论中的假设检验。

---

### 2.2 运行结果

系统在典型场景下的实测数据如下（测试方法见第 3 章）：

**无攻击场景**
- 良好信道（400 km 天顶，晴天）：QBER 约 0.65%，安全密钥率约 33.5 kbps，安全等级 HIGH。
- 中等信道（600 km，薄雾）：QBER 约 2.54%，安全密钥率约 0.5 kbps，安全等级 MEDIUM。
- 恶劣信道（700 km，雨天）：QBER 约 22.6%，安全密钥率 0 kbps（超过 11% 阈值），安全等级 CRITICAL。

**攻击场景**
- 截获-重发 50% 强度：QBER 升至约 9.33%，接近 11% 安全边界，安全密钥率归零。
- 光束分离 30% 分光比：QBER 维持约 0%，但 Eve 信息比例约 16.2%，隐私放大后安全密钥率显著下降。
- PNS 攻击 + 无诱骗态：QBER 正常（约 0.81%），但系统判定密钥不安全，安全密钥率强制归零并输出警告。
- PNS 攻击 + 启用诱骗态：系统检测到产额异常，改用 GLLP 保守公式计算，安全密钥率恢复但低于正常值。

---

### 2.3 技术指标

| 指标 | 数值/范围 | 说明 |
|------|----------|------|
| 脉冲重复频率 | 100 MHz | 弱脉冲光源 |
| 平均光子数 $\mu$ | 0.8 | 兼顾计数率与多光子比例 |
| 探测器效率 | 40% | 单光子探测器 |
| 最大可接受 QBER | 11% | BB84 协议安全硬阈值 |
| 通信波长 | 850 nm | 大气窗口 |
| 暗计数率 | 50 Hz | 探测器暗噪声 |
| 安全密钥率（400 km 天顶晴天） | ~33.5 kbps | 实测值 |
| 单次 QKD 仿真延迟（10000 脉冲） | ~5.6 ms | 良好信道 |
| 支持时间点数 | >2000 | 90 分钟过境，60 秒步长 |

---

*文档版本：v2.0（对应代码修订版）*  
*修订内容：新增隐私放大教学演示、修正 PNS 攻击逻辑、安全阈值改为二元硬阈值判定、补充公式来源与课程关联说明*
