# 论文阅读报告：SQIsign2D² 数字签名方案
## ——基于一维光滑同源的 SQIsign2D 优化变种

> **论文信息**
> - **作者**：Zheng Xu, Kaizhan Lin, Chang-An Zhao, Yi Ouyang
> - **来源**：ASIACRYPT 2025, Part IV, LNCS, pp. 341–371. Springer, Singapore, December 2025
> - **预印本**：IACR Cryptology ePrint Archive, Report 2025/920, 2025
> - **标题**：SQIsign2D²: New SQIsign2D Variant by Leveraging Power Smooth Isogenies in Dimension One

---

## 一、引言

### 1.1 研究背景

2024年8月，NIST（美国国家标准与技术研究院）正式发布首批后量子密码标准，标志着密码学进入"后量子时代"。在多条后量子技术路线中，**基于同源的密码学（Isogeny-based Cryptography）** 因其独特的数学结构而备受关注。

同源密码学的核心优势在于：目前尚无已知量子算法能在多项式时间内求解超奇异椭圆曲线同源问题。这与基于格的方案形成对比——格密码虽能抵抗已知量子攻击，但其安全性依赖于若干启发式假设。同源问题的数学基础更为经典，与椭圆曲线密码学（ECC）渊源深厚，同时又能天然抵抗 Shor 算法的攻击。

### 1.2 SQIsign 与同源数字签名

**SQIsign** 由 De Feo 等人于 2020 年提出，是目前唯一进入 NIST 第二轮评估的同源签名方案。其最大优势是签名长度极短（约 200 字节，与 ECC 相当），但计算效率较低——签名和验证过程需进行复杂的"理想-同源转换"。

为突破这一瓶颈，近年来陆续出现了 SQIsignHD、SQIsign2D-West/East 等改进方案，而本文介绍的 **SQIsign2D²** 则是最新的优化变种。

### 1.3 本文核心贡献

SQIsign2D² 通过重新设计素数参数与创新算法，实现了目前最快的 SQIsign2D 变体。在 NIST-I 安全级别下，与 SQIsign2D-East 相比：

| 操作阶段 | SQIsign2D-East | SQIsign2D² (本文) |
|:------:|:-------------:|:----------------:|
| 密钥生成 | 560 ms | 213 ms (快 2.6 倍) |
| 签名 | 1263 ms | 587 ms (快 2.1 倍) |
| 验证 | 296 ms | 247 ms (快 1.2 倍) |

其核心洞见在于：利用**一维光滑同源**（3 的幂次）来降低二维同源的计算开销，体现了"降维优化"的算法设计思想。

---

## 二、技术背景

### 2.1 椭圆曲线同源基础

同源（Isogeny）是椭圆曲线之间的特殊映射，在保持群结构的同时将一条曲线映射到另一条曲线。对于超奇异椭圆曲线，其自同态环与四元数代数中的最大序之间存在一一对应关系，即著名的 **Deuring 对应**。

同源密码学的主要计算瓶颈是**理想-同源转换**：需要将四元数理想中的元素显式计算为曲线上的有理映射。这正是 SQIsign 家族效率低下的根源。

### 2.2 高维同源与 Kani 引理

**Kani 引理**是近年来同源密码学的重要工具。它允许我们通过构造二维同源（即阿贝尔簇之间的同源）来分解和计算一维同源。SQIsign2D 家族的签名方案均基于这一思想：借助二维同源计算，回避直接进行代价高昂的理想-同源转换。

### 2.3 SQIsign2D-East 简介

作为对比基准，SQIsign2D-East 采用形如 $p = 2^{a+b} \cdot f - 1$ 的素数，其中 $f$ 为小因子。这种参数设置使得 2 的幂次挠子群可被高效利用，但核心计算仍依赖二维 $(2^e, 2^e)$-同源，成本较高。

---

## 三、SQIsign2D² 的核心创新

### 3.1 参数重构：$p = C \cdot D - 1$

本文的关键创新在于改变了素数的选取方式。传统方案使用 $p = f \cdot 2^a - 1$，仅利用 2 的幂次；而 SQIsign2D² 采用：

$$
p = 2^{e_2} \cdot 3^{e_3} - 1
$$

其中 $C = 3^{e_3} \approx D = 2^{e_2} \approx \sqrt{p}$。这一设计的优势在于：

1. $E_0[C]$ 和 $E_0[D]$ 均为有理挠子群（定义在基域上）
2. $C$-同源和 $D$-同源都可用 Vélu 公式高效计算（一维）
3. 二维同源的度数需求从 $\sim p$ 降至 $\sim \sqrt{p}$

### 3.2 算法一：ImRanIso（改进随机同源生成）

该算法用于密钥生成。其思路是：先构造一个度数为 $d(D/D'-d) \cdot C \cdot D'$ 的内自同态，再通过二维同源分解得到目标 $d$-同源。相比前人工作，该算法巧妙地利用 $CD'$-同源（其中 $D' = 2^{\lfloor e_2/2 \rfloor} \approx p^{1/4}$）来降低二维同源的度数需求。

**直观理解**：以适当增加一维同源的计算量（约 $\frac{3}{4} \log_2 p$）为代价，大幅降低二维同源的复杂度（降至约 $\frac{1}{2} \log_2 p$），从而实现整体加速。

### 3.3 算法二：GenImRanIso（广义改进随机同源）

该算法用于签名阶段生成辅助同源。难点在于：此时从曲线 $E_A$ 出发，其自同态环未知。本文的创新在于利用 **Eichler 序（Eichler order）** 构造所需的内自同态，而非使用完整的最大序。

**关键优势**：完全避免了 East 方案中需要的 $(2^e, 2^e)$-二维同源，转而使用更易计算的 $(D,D)$-二维同源。

### 3.4 响应同源条件的简化

East 方案要求响应同源的度数 $q$ 满足 $(2^a, 2^b)$-nice 的复杂条件。本文将其简化为仅需 **D-adequate**：$q$ 为奇数、$q < D$、且 $3 \nmid q$。简化后采样成功率从约 $1/8$ 提升至约 $1/4$。

---

## 四、SQIsign2D² 协议流程

SQIsign2D² 是一个 Σ-协议（识别协议），可通过 Fiat-Shamir 变换转换为数字签名方案。

### 4.1 参数设置

以 NIST-I 安全级别为例，选取 $p = 2^{131} \cdot 3^{78} - 1$，这是一个约 256 比特的素数。公钥约 64 字节，签名约 154 字节（压缩版）。

### 4.2 密钥生成

1. 选取随机素数 $N_\tau < p^{1/4}$，满足 Legendre 符号 $\left(\frac{3}{N_\tau}\right) = -1$（用于抵抗密钥恢复攻击）
2. 使用 ImRanIso 算法生成 $N_\tau$-同源 $\tau: E_0 \to E_A$
3. 公钥为曲线 $E_A$，私钥包含同源 $\tau$ 的信息

### 4.3 承诺-挑战-响应

**承诺**：证明者使用 ImRanIso 生成随机同源 $\psi: E_0 \to E_1$，发送承诺曲线 $E_1$。

**挑战**：验证者发送随机点 $K_{\text{cha}} \in E_1[C]$ 作为挑战。

**响应**：证明者计算挑战同源 $\varphi: E_1 \to E_2$，然后构造响应同源 $\sigma: E_A \to E_2$。利用 GenImRanIso 生成辅助同源，最终返回压缩后的响应。

### 4.4 验证

验证者通过计算 $(D,D)$-二维同源来验证响应的有效性。若同源分解后得到的 $\sigma$ 满足条件，则接受。

---

## 五、安全性分析

### 5.1 特殊可靠性

若证明者能对两个不同挑战生成有效响应，则可提取出 $E_A$ 的非平凡自同态，进而可能恢复密钥。这保证了协议的可靠性。

### 5.2 零知识性

在随机预言机模型下，协议满足特殊诚实验证者零知识。核心假设是：ImRanIso 输出的承诺曲线与随机超奇异曲线计算不可区分。

### 5.3 承诺采样的强化

针对上述启发式假设，论文提出两种强化方案：
- **Double Path 技术**：将承诺同源度数提升至 $\sim p$，增强随机性
- **$(3,3)$-同源适配**：直接计算 $(CD, CD)$-二维同源

---

## 六、性能评估与对比

实验在 Intel Core i9-12900K 上使用 Julia 实现。核心发现如下：

- SQIsign2D² 用一维 3-同源替代了大量二维 $(2,2)$-同源，这是效率提升的关键
- 密钥生成阶段，$(2,2)$-同源计算从 253 步降至 66 步
- 签名阶段，$(2,2)$-同源计算从 641 步降至 262 步

与同期工作 SQIsign2DPush 相比，本文方案在承诺生成阶段依赖更强的安全假设，但效率更高。与 PRISM-sig 相比，本文方案验证更快。

---

## 七、个人思考与展望

### 7.1 量子信息视角的解读

作为量子信息专业的学生，我对同源密码学的量子安全性有一些思考。Shor 算法能破解 RSA/ECC，是因为它们依赖于**阿贝尔群上的隐藏子群问题（HSP）**。然而，超奇异同源问题对应的是**非阿贝尔群**（四元数代数理想类群），目前尚无有效的量子算法。

本文通过增加一维光滑同源的使用来优化性能，这一策略的量子安全性很有意思：
- 一维光滑同源的经典计算已很高效（Vélu 公式），量子计算也没有明显优势
- 二维同源即使对量子计算机仍是难题

这种"经典-量子"的权衡设计值得深入研究。

### 7.2 对论文的评价

**优点**：
1. **问题定位准确**：作者精准识别出二维同源是效率瓶颈，而非盲目优化
2. **工程实现完整**：提供了 Julia 语言的完整实现，实验数据详实
3. **理论-实践平衡**：在安全性假设与效率之间取得了较好的权衡

**可改进之处**：
1. 承诺生成的随机性假设缺乏严格证明，虽有强化方案但会增加开销
2. 即便优化后，签名时间（587 ms）仍远高于 NIST 标准的 ML-DSA（< 1 ms）——当然签名长度优势明显（154 B vs 2420 B）

### 7.3 未来展望

**短期**（1–2 年）：
- 进一步优化 $(3,3)$-同源的实现，使安全强化方案更实用
- 探索 GPU/FPGA 加速，特别是 KaniCod 算法的并行化

**中期**（3–5 年）：
- 研究同源密码与其他后量子方案（如格密码）的混合协议，实现双重保护
- 后量子 TLS/SSH 的集成工程问题

**长期**（5 年以上）：
- 当容错量子计算真正实现时，同源密码是否仍安全？
- 这需要量子复杂性理论的突破，特别是**非阿贝尔隐藏子群问题**的量子难度证明

---

## 八、结论

SQIsign2D² 通过参数重构（$p = 2^{e_2} \cdot 3^{e_3} - 1$）与算法创新（ImRanIso / GenImRanIso），将 SQIsign2D 家族的效率提升到了新高度。其核心洞见——用一维光滑同源替代高维同源计算——不仅具有直接的工程价值，也为后量子密码学的"经典-量子"资源权衡提供了新思路。

作为量子信息专业的学生，我认为本文最重要的启示是：**量子安全不等于量子低效**。通过精细的算法设计，完全可以在保持抗量子安全性的同时，实现可实用的计算效率。

---

## 参考文献

[1] NIST. Post-Quantum Cryptography Standardization. 2024.

[2] De Feo L, et al. SQIsign: Compact Post-Quantum Signatures from Quaternions and Isogenies. ASIACRYPT 2020.

[3] Dartois P, et al. SQIsignHD: New Dimensions in Cryptography. EUROCRYPT 2024.

[4] Nakagawa K, et al. SQIsign2D-East: A New Signature Scheme Using 2-Dimensional Isogenies. ASIACRYPT 2024.

[5] Xu Z, Lin K, Zhao C A, Ouyang Y. SQIsign2D²: New SQIsign2D Variant by Leveraging Power Smooth Isogenies in Dimension One. ASIACRYPT 2025, Part IV, LNCS, pp. 341–371. Springer, Singapore, 2025.

[6] Castryck W, et al. Breaking and Repairing SQIsign2D-East. ePrint 2024/1453.

[7] Kani E. The number of curves of genus two with elliptic differentials. J. Reine Angew. Math. 1997.
